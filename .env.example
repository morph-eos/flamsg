# FlaMSG Environment Variables Configuration
# Copy this file to .env and fill in your actual values

# MongoDB Connection String
# For local MongoDB installation:
MONGODB_URI=mongodb://localhost:27017/flamsg

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/flamsg?retryWrites=true&w=majority

# Server Configuration
PORT=3000

# Development Notes:
# - Never commit your actual .env file to version control
# - The .gitignore file already excludes .env files
# - Make sure MongoDB is running before starting the server
# - For production deployment, set these as environment variables on your hosting platform
